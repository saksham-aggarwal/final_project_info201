library(shiny)

# Define UI for application that draws a histogram
shinyUI(
  fluidPage(
  # Application title
    titlePanel("Getting Real About Fake News"),

    # Sidebar for user input
    sidebarLayout(
      
      sidebarPanel(
        # Insert sidebar input her
      ),
      
      mainPanel(
        # Insert plots and data here
      )
    )
  )
)